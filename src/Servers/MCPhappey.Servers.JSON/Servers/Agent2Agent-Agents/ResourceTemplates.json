{
  "resourceTemplates": [
    {
      "uriTemplate": "http://localhost:3025/agents.json",
      "name": "List all available agents on the server",
      "description": "List all available agents on the server",
      "mimeType": "application/json",
      "annotations": {
        "audience": ["assistant"]
      }
    }
  ]
}
