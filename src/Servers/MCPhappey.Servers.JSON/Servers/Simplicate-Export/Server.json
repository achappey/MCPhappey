{
  "serverInfo": {
    "name": "Simplicate-Export",
    "version": "1.0.0",
    "description": "CSV-exportintegratie voor Simplicate om asynchrone datadumps aan te vragen. Retourneert een download-URL i.p.v. inline data."
  },
  "headers": {
    "Authentication-Key": "Simplicate Api Key",
    "Authentication-Secret": "Simplicate Api Secret"
  },
  "plugins": [
    "MCPhappey.Simplicate.Export.SimplicateExport, MCPhappey.Simplicate"
  ],
  "instructions": "Use the locale delimeter from user context when known. Always use the select query parameter for the fields needed. Only use the export tool when CSV export files are explicitly asked. Don't use Export tool to search/retrieve data, since it will only return the url to the csv file, not the content itself.",
  "obo": {
    "graph.microsoft.com": "https://graph.microsoft.com/User.Read https://graph.microsoft.com/Sites.ReadWrite.All"
  }
}
