// Core React component structure for MCP Happey apps (no styling)
// Add main component structure, slots, ARIA, etc. here

import * as React from "react";

export function ExampleCoreComponent({  }: {  }) {
  return <div role="main">Test</div>;
}
